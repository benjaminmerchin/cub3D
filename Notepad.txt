Liste des fonctions de la mlx :

Categorie 1 mlx_loop : (boucle qui lisent des donnees en entree et maintiennent le prog actif)
    int mlx_loop ( void *mlx_ptr );
    int mlx_key_hook ( void *win_ptr, int (*funct_ptr)(), void *param );
    int mlx_mouse_hook ( void *win_ptr, int (*funct_ptr)(), void *param );
    int mlx_expose_hook ( void *win_ptr, int (*funct_ptr)(), void *param );
    int mlx_loop_hook ( void *mlx_ptr, int (*funct_ptr)(), void *param );

Categorie 2 mlx_new_image : (gestion de la creation d'images, destructions et couleurs)
    void *mlx_new_image ( void *mlx_ptr, int width, int height );
    char *mlx_get_data_addr ( void *img_ptr, int *bits_per_pixel, int *size_line, int *endian );
    int mlx_put_image_to_window ( void *mlx_ptr, void *win_ptr, void *img_ptr, int x, int y );
    unsigned int mlx_get_color_value ( void *mlx_ptr, int color );
    void *mlx_xpm_to_image ( void *mlx_ptr, char **xpm_data, int *width, int *height );
    void *mlx_xpm_file_to_image ( void *mlx_ptr, char *filename, int *width, int *height );
    int mlx_destroy_image ( void *mlx_ptr, void *img_ptr );

Categorie 3 mlx_new_window : (gerer la creation de fenetres)
    void *mlx_new_window ( void *mlx_ptr, int size_x, int size_y, char *title );
    int mlx_clear_window ( void *mlx_ptr, void *win_ptr );
    int mlx_destroy_window ( void *mlx_ptr, void *win_ptr );

Categorie 4 mlx_pixel_put : (afficher des pixel a l'ecran)
    int mlx_pixel_put ( void *mlx_ptr, void *win_ptr, int x, int y, int color );
    int mlx_string_put ( void *mlx_ptr, void *win_ptr, int x, int y, int color, char *string );

Idees :
>

Todo :
> Rajouter dans le parsing la bonne ouverture des images en essayant directement de les ouvrir et bien verifier 
que ce n'est pas un doossier
> Verifier le nombre d'argument soit seulement la map soit la map et --save
> Bien verifier que je limite la taille de l'ecran a 20000 px en largeur (avec --save) pour que ca fonctionne avec les sprites

Divers :
>Les segfaults : utiliser -fsanitize=address avec les flags du Makefile
>Je realise le projet sur mac avec comme idee de le transiter vers linux a la fin. La version linux comporte une fonctin en plus
qui permet de ne plus avoir de leaks still reachable, fonction qui n'existe pas sur la version mac. L'idee etant que le prog
detecte de lui-meme l'OS sur lequel il se trouve et se compile de maniere adecouate.
>C'est quoi le & ?



Bonus list Goal : 14 Bonus -> 115%
>vCollision contre les murs
• Une skybox.
• Texture sur le sol ou le plafond.
> Un HUD.
• Possibilité de regarder en haut et en bas.
> Sauter ou s’accroupir.
> Un effet d’ombre basé sur la distance.
> Une barre de vie.
> Plus d’objets dans le labyrinthe.
> Collision avec les objets.
> Gain ou perte de points/de vie en prenant des objets/pièges.
> Porte qui peuvent être ouvertes/fermées.
> Portes secrètes.
> Animations (tir d’arme ou des sprites animés).
> Plusieurs niveaux.
• Son et musique.
> Des armes et des méchants à combattre !


Minimap n'est pas dans les bonus ?